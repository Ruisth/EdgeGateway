name: Build Yocto

on:
  push:
    branches: [main]
    paths:
      - "yocto/**"
  workflow_dispatch:

# GitHub deprecates Node 20 action runtimes on 2026-06-16; opt into the
# Node 24 default ahead of the forced migration.
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  build:
    name: Build Yocto Image
    runs-on: [self-hosted, yocto]
    steps:
      - uses: actions/checkout@v4

      - name: Setup build environment
        run: |
          source oe-init-build-env build
          bitbake-layers add-layer ../yocto/layers/meta-edgegateway

      - name: Build edgegateway-image
        run: |
          source oe-init-build-env build
          bitbake edgegateway-image

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: yocto-image
          path: build/tmp/deploy/images/
          retention-days: 7
